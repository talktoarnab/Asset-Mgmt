services:
  dynamodb:
    image: amazon/dynamodb-local:latest
    container_name: shelfkit-dynamodb
    command: ['-jar', 'DynamoDBLocal.jar', '-sharedDb', '-inMemory']
    ports:
      - '8000:8000'
    healthcheck:
      test: ['CMD-SHELL', 'curl -sf http://localhost:8000 || exit 1']
      interval: 10s
      timeout: 3s
      retries: 5
